# Third Party

Third-party source drops, generated dependency files, and local SDK extracts
belong here only when a prompt explicitly requires them.

Do not commit massive CEF binary distributions directly. Keep local CEF
extracts ignored and document the selected version in `THIRD_PARTY_NOTICES.md`
before packaging.
